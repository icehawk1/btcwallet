package de.mhaug.phd.btcwallet

class BitcoinAddress

class Base58Parser {
    fun encode(raw: ByteArray, checksum: Boolean = true): String = TODO()
    fun decode(encoded: String, checksum: Boolean = true): ByteArray = TODO()
}
